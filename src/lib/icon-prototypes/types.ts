/** Metadata and inline SVG markup for one icon prototype. */
export interface IconPrototype {
  id: string;
  title: string;
  concept: string;
  agent: string;
  style: string;
  svg: string;
  tags: string[];
}
